# Stub for Utils — actual render logic was removed; v5-2 only references it in commented-out debug code
class Utils:
    @staticmethod
    def render(state, use_color=False):
        return str(state)
